import { BlockHeightDirective } from './block-height.directive';

describe('BlockHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockHeightDirective();
    expect(directive).toBeTruthy();
  });
});
